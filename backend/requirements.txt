# Runtime dependencies, pinned to the versions verified together on
# Python 3.12 / Windows on 2026-09-10 (`pytest` green, all imports load).
# Bump deliberately and re-run the test suite; do not loosen to `>=`.

fastapi==0.141.1
starlette==1.6.0              # imported directly by app.middleware
uvicorn==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
python-dotenv==1.2.3
bcrypt==5.0.0
PyJWT==2.13.0
cryptography==50.0.1
pyotp==2.10.0
qrcode==8.2
SQLAlchemy==2.0.52
groq==1.7.0
httpx==0.28.1

# Computer vision / inference
opencv-python==4.11.0.86
# numpy must stay on 1.x while mediapipe is pinned to 0.10.14 (NumPy 1.x ABI);
# an unbounded numpy resolves to 2.x and mediapipe fails to import.
numpy==1.26.4
pillow==12.3.0
# 0.10.14 is the last wheel line that still ships the legacy `mp.solutions`
# API used by face_recognizer.py and gate_attendance.py. driver.py already uses
# the Tasks FaceLandmarker; porting the two legacy call sites unlocks 1.x.
mediapipe==0.10.14
# AGPL-3.0. See README "License & third-party licences".
ultralytics==8.4.146
onnxruntime==1.29.0
# 2.0 installs without a C++ compiler. Pretrained model packs (buffalo_l) are
# licensed for non-commercial research use only.
insightface==2.0
