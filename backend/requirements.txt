# Core (install first)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
bcrypt>=4.1.2
PyJWT>=2.8.0
cryptography>=42.0.0
pyotp>=2.9.0
qrcode>=7.4.2

# Database (without greenlet/async)
sqlalchemy>=2.0.25

# Chatbot (safety assistant)
groq>=0.31.0
httpx>=0.27.0

# AI/ML - Phase 1 (no build tools needed)
opencv-python>=4.9.0
numpy>=1.24.0
pillow>=10.0.0

# MediaPipe for face/pose detection.
# IMPORTANT: mediapipe 0.10.15+ removed the `mp.solutions.*` API on some
# platforms (only `mp.tasks` remains) and our live face-box overlay uses
# `mp.solutions.face_detection.FaceDetection`. Pin to the last version that
# still ships the solutions API to avoid silent overlay failures.
mediapipe>=0.10.9,<0.10.15

# AI/ML — PPE detection (YOLOv8)
ultralytics>=8.1.0
onnxruntime>=1.16.0

# AI/ML — Face recognition (ArcFace).
# NOTE: requires Microsoft C++ Build Tools on Windows. If the install fails,
# install the Visual Studio Build Tools first, then re-run pip install.
insightface>=0.7.3
